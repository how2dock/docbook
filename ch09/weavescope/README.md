A vagrantbox to play with [Weavescope](https://github.com/weaveworks/scope)

    $ vagrant up
    $ vagrant ssh
    $ cd /vagrant
    $ docker-compose up -d
    $ ./scope launch

Now open your browser at `http://192.168.33.10:4040` and explore
